- 所有核心文档和设计在 docs 目录下，所有新需求开发必须先更新 docs 下各文档，包括plan和测试用例
- 功能开发完成后，要进行测试，必须保证测试代码覆盖率不低于80%，并且所有测试用例必须通过，必须完成e2e测试
- 测试必须验证功能是否真正工作，而不仅仅是 UI 元素是否存在，也要关联测试对应的接口和功能，特别是涉及数据的crud和对应的界面e2e测试。初始测试通过不代表功能正常。
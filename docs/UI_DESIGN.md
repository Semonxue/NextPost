# NextPost UI 设计文档

## 概述

本文档描述 NextPost 的用户界面设计，包括页面清单、典型用户体验流程、以及核心组件设计。

---

## 页面清单

### 第一期（MVP）页面

| 序号 | 页面名称 | 路由 | 描述 | 优先级 |
|------|---------|------|------|--------|
| 1 | 登录页 | `/login` | 用户登录 | P0 |
| 2 | 注册页 | `/register` | 用户注册 | P0 |
| 3 | 仪表盘 | `/` | 概览统计和快捷入口 | P0 |
| 4 | 账号管理 | `/accounts` | Twitter 账号列表和添加 | P0 |
| 5 | 内容创作 | `/posts/new` | 创建新帖子 | P0 |
| 6 | 帖子编辑 | `/posts/:id/edit` | 编辑已有帖子 | P0 |
| 7 | 日历视图 | `/calendar` | 按日历展示发布计划 | P0 |
| 8 | 列表视图 | `/posts` | 表格形式管理所有帖子 | P0 |
| 9 | AI tools | `/ai-tools` | MCP 配置 + 工具列表（v0.4）| P1 |
| 10 | 设置页 | `/settings` | 用户设置和 API Key 管理 | P1 |
| 11 | 回收站 | `/trash` | 软删除的帖子/账号列表 + 恢复 / 永久删除 | P1 |
| 12 | AI 对话（Phase 2）| `/chat` | NextPost 内部 AI 助手（规划中）| P2 |

### 导航结构

```
侧边栏导航
├── 仪表盘 (Dashboard)
├── 日历视图 (Calendar)
├── 帖子列表 (Posts)
├── 账号管理 (Accounts)
├── AI tools                ← v0.4 新增（MCP 配置 + 工具列表）
├── 设置 (Settings)
└── 回收站 (Trash)
```

---

## 典型用户体验流程

### 流程 1：创建发布计划

```
用户登录
    ↓
进入仪表盘
    ↓
点击「新建帖子」按钮
    ↓
┌─────────────────────────────┐
│  内容创作页                   │
│  - 选择账号                   │
│  - 输入文本内容               │
│  - 上传媒体（可选）           │
│  - 设置发布时间               │
└─────────────────────────────┘
    ↓
点击「保存为草稿」或「立即发布」
    ↓
返回列表视图或日历视图
```

### 流程 2：AI 辅助编辑

```
用户进入 AI 对话页
    ↓
发送指令："帮我查看这周的计划"
    ↓
AI 调用工具获取数据
    ↓
展示结果："这周有 3 个计划..."
    ↓
用户继续："把明天下午3点的改成5点"
    ↓
AI 调用工具修改
    ↓
确认修改成功
```

### 流程 3：账号管理

```
用户进入账号管理页
    ↓
点击「添加账号」按钮
    ↓
┌─────────────────────────────┐
│  添加账号弹窗                 │
│  - 账号名称（自定义）         │
│  - Twitter Handle（@xxx）    │
│  - 备注描述（可选）           │
└─────────────────────────────┘
    ↓
点击保存
    ↓
账号出现在列表中
```

---

## 页面详细设计

### 1. 登录页 / 注册页

**设计要点：**
- 简洁的表单布局
- 用户名 + 密码（无验证码）
- 注册时确认密码
- 切换登录/注册链接

**布局示意：**
```
┌──────────────────────────────────────┐
│                                      │
│            NextPost Logo             │
│                                      │
│  ┌────────────────────────────────┐  │
│  │  用户名                         │  │
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │  密码                           │  │
│  └────────────────────────────────┘  │
│                                    │
│  ┌────────────────────────────────┐  │
│  │         登录                    │  │
│  └────────────────────────────────┘  │
│                                    │
│      还没有账号？注册              │
│                                      │
└──────────────────────────────────────┘
```

### 2. 仪表盘

**设计要点：**
- 左侧侧边栏导航
- 顶部统计卡片（本周计划数、待发布、已发布）
- 中间快捷操作按钮（新建帖子、查看日历）
- 近期计划列表

**布局示意：**
```
┌────────┬──────────────────────────────────────────────┐
│        │  仪表盘                                      │
│  LOGO  │                                              │
│        │  ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│ ────── │  │ 本周计划 │ │ 待发布   │ │ 已发布   │      │
│ 📊 仪表 │  │    12    │ │    5     │ │    7     │      │
│ 📅 日历 │  └──────────┘ └──────────┘ └──────────┘      │
│ 📝 帖子 │                                              │
│ 👤 账号 │  ┌─────────────────┐ ┌─────────────────┐   │
│ 🤖 AI   │  │  + 新建帖子      │ │  📅 查看日历     │   │
│ ⚙ 设置 │  └─────────────────┘ └─────────────────┘   │
│        │                                              │
│        │  近期计划                                    │
│        │  ┌─────────────────────────────────────┐   │
│        │  │ 周三 15:00 - 产品发布公告      [@账号] │   │
│        │  │ 周四 09:00 - 早安推文          [@账号] │   │
│        │  │ 周五 20:00 - 周末活动预告      [@账号] │   │
│        │  └─────────────────────────────────────┘   │
└────────┴──────────────────────────────────────────────┘
```

### 3. 内容创作页

**设计要点：**
- 表单式布局
- 账号选择下拉框
- 富文本编辑器（支持 Markdown）
- 媒体上传区域
- 日期时间选择器
- 保存/取消按钮

**布局示意：**
```
┌────────┬──────────────────────────────────────────────┐
│        │  新建帖子                                     │
│        │                                              │
│        │  ┌────────────────────────────────────────┐ │
│        │  │ 选择账号 ▼                              │ │
│        │  └────────────────────────────────────────┘ │
│        │                                              │
│        │  ┌────────────────────────────────────────┐ │
│        │  │                                        │ │
│        │  │  输入帖子内容...                         │ │
│        │  │  支持 Markdown                         │ │
│        │  │                                        │ │
│        │  └────────────────────────────────────────┘ │
│        │                                              │
│        │  ┌────────────────────────────────────────┐ │
│        │  │  📷 上传图片/视频                        │ │
│        │  └────────────────────────────────────────┘ │
│        │                                              │
│        │  发布时间                                    │
│        │  ┌────────────────────┐ ┌────────────────┐  │
│        │  │ 2024-01-15  15:00  │ │ Asia/Shanghai ▼│  │
│        │  └────────────────────┘ └────────────────┘  │
│        │                                              │
│        │  ┌────────────┐  ┌────────────┐            │
│        │  │  保存草稿   │  │   发布     │            │
│        │  └────────────┘  └────────────┘            │
└────────┴──────────────────────────────────────────────┘
```

### 4. 日历视图

**设计要点：**
- 月视图为主，支持周视图切换
- 日期格子显示当天计划数量
- 点击日期展开当天计划列表
- 拖拽调整时间（可选）

**布局示意：**
```
┌────────┬──────────────────────────────────────────────┐
│        │  日历视图            [月] [周]  < 1月 2024 >  │
│        │                                              │
│        │    日   一   二   三   四   五   六           │
│        │  ┌───┬───┬───┬───┬───┬───┬───┐             │
│        │  │   │   │   │ 1 │ 2 │ 3 │ 4 │             │
│        │  │   │   │   │ 2 │   │ 1 │   │             │
│        │  ├───┼───┼───┼───┼───┼───┼───┤             │
│        │  │ 7 │ 8 │ 9 │10 │11 │12 │13 │             │
│        │  │ 1 │   │ 3 │   │   │ 2 │   │             │
│        │  ├───┼───┼───┼───┼───┼───┼───┤             │
│        │  │14 │15 │16 │17 │18 │19 │20 │             │
│        │  │   │ 5 │   │   │   │   │ 1 │             │
│        │  └───┴───┴───┴───┴───┴───┴───┘             │
│        │                                              │
│        │  1月15日 (周一)                             │
│        │  ┌─────────────────────────────────────┐     │
│        │  │ 09:00 - 早安推文              [@账号] │     │
│        │  │ 15:00 - 产品发布公告          [@账号] │     │
│        │  │ 20:00 - 周末活动预告          [@账号] │     │
│        │  └─────────────────────────────────────┘     │
└────────┴──────────────────────────────────────────────┘
```

### 5. 列表视图

**设计要点：**
- 表格形式展示
- 列：内容预览、账号、发布时间、状态、操作
- 支持筛选（账号、状态、日期范围）
- 支持排序
- 分页（使用 `Pagination` 组件）

**分页组件功能：**
- 显示当前页显示范围（如：显示 1-20，共 50 条）
- 每页条数选择器（10/20/50/100 条）
- 上一页/下一页按钮
- 最多显示 5 个页码按钮，支持智能省略号

**布局示意：**
```
┌────────┬──────────────────────────────────────────────┐
│        │  帖子列表                        [+ 新建帖子] │
│        │                                              │
│        │  筛选：账号 ▼  状态 ▼  日期范围              │
│        │                                              │
│        │  ┌────┬────────┬──────┬────────┬────┬────┐  │
│        │  │状态│ 内容   │ 账号  │ 发布时间│操作│    │  │
│        │  ├────┼────────┼──────┼────────┼────┼────┤  │
│        │  │ 📅 │ 产品发 │@小号A│ 1-15 15:00│编辑│删除│  │
│        │  │ 📅 │ 早安推 │@小号B│ 1-16 09:00│编辑│删除│  │
│        │  │ ✅ │ 周末活 │@小号A│ 1-12 20:00│编辑│删除│  │
│        │  │ 📝 │ 草稿内 │@小号C│   -      │编辑│删除│  │
│        │  └────┴────────┴──────┴────────┴────┴────┘  │
│        │                                              │
│        │  显示 1-20，共 50 条  [每页 ▼ 20]  < 1 2 3 >│
└────────┴──────────────────────────────────────────────┘
```

**回收站分页：**
- 帖子和账号 Tab 各自独立分页
- 切换 Tab 时重置到第一页
- Tab 显示对应类型的总数量

### 5b. AI tools 页（v0.4 新增）— 替代原 `/chat` 占位

> **设计目的**：让用户**真实看到**外部 AI 工具能调到的所有 MCP 工具，并提供各客户端的配置示例。**关键：工具列表不是硬编码，而是 Server Component 在请求时从 `src/mcp/external/tools.ts` 的 `TOOLS` 常量加载**——这跟 `/api/mcp` 接口的 source of truth 是同一份。

**路由**：`/ai-tools`（Server Component，强制 `dynamic = 'force-dynamic'`）

**布局（自上而下）**：

```
┌─────────────────────────────────────────────────────────────┐
│ 🔧 AI tools                                                  │
│  副标题：NextPost 通过 MCP 对外暴露 N 个工具，让外部 AI ...   │
│  标签：[MCP v0.4] [工具数：9] [读取：4] [写入：5]              │
├─────────────────────────────────────────────────────────────┤
│ 1. MCP 配置                                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ MCP 端点：POST http://localhost:3456/api/mcp           │  │
│  │                                                        │  │
│  │ 你的 API Key (N) → [去设置管理]                        │  │
│  │  本页只展示摘要（不展示 key 明文 / Reveal）            │  │
│  │  跳转 /settings 看完整 key、reveal、删除、新建          │  │
│  │                                                        │  │
│  │ 客户端配置示例（4 个折叠面板）                          │  │
│  │  ▶ Claude Desktop                                      │  │
│  │  ▶ Cursor / Cline / Continue                          │  │
│  │  ▶ Cherry Studio                                       │  │
│  │  ▶ VS Code (.vscode/mcp.json)                         │  │
│  │    每个面板含可复制的 JSON 配置                         │  │
│  │                                                        │  │
│  │ 权限范围表（read / write / read_write）                 │  │
│  └────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 2. MCP 工具（9 个，实时从 src/mcp/external/tools.ts 加载）   │
│  读取工具（4）                                               │
│  ┌─ list_accounts [read] ───────────────────────────┐       │
│  │ 获取账号列表（脱敏）                              │       │
│  │ inputSchema: { ... }                              │       │
│  └──────────────────────────────────────────────────┘       │
│  ┌─ get_pending_posts [read] ───────────────────────┐       │
│  ...                                                         │
│  写入工具（5，需 write / read_write scope）                  │
│  ┌─ upload_media_from_url [write] ──────────────────┐       │
│  ┌─ upload_media_from_path [write] ─────────────────┐       │
│  ┌─ upload_media_from_base64 [write] ───────────────┐       │
│  ┌─ create_post [write] ─────────────────────────────┐       │
│  ┌─ update_post [write] ─────────────────────────────┐       │
├─────────────────────────────────────────────────────────────┤
│ 3. 写工具安全约束                                             │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ ⚠ 不提供 delete：外部 MCP 没有任何删除工具               │  │
│  │ ⚠ update_post 字段白名单：v0.4.2 起 content / mediaUrls /│  │
│  │   scheduledTime / timezone 可改，accountId / status 忽略 │  │
│  │ ⚠ update_post 状态锁：仅 draft/scheduled 可改            │  │
│  │ ⚠ upload_media_from_url 限制：http/https、≤10MB、6 mime │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**数据来源（关键 — 全部真实，非硬编码）**：

| 数据 | 来源 | 客户端可见 |
|------|------|----------|
| 工具列表 + inputSchema | `import { TOOLS } from "@/mcp/external/tools"` | ✅ |
| 工具 scope 标签 | `import { TOOL_SCOPE } from "@/mcp/external/tools"` | ✅ |
| 用户的 API Key 数量 | `prisma.externalApiKey.count({where:{userId}})` | ✅（仅计数） |
| 完整 API Key 管理 | 跳转到 `/settings` | —（不在本页） |

> **安全性 v0.4 变更**：本页**不再展示**完整 key、preview 或 Reveal 按钮。完整 key 管理（reveal / 复制 / 删除 / 新建）全部在 `/settings` 完成。这避免在多处泄露 key 明文。

**交互（client components）**：
- `CopyButton`（`src/app/(main)/ai-tools/CopyButton.tsx`）：通用文本复制到剪贴板（带 fallback for non-secure contexts）

**E2E 覆盖**（`tests/e2e/ai-tools.spec.ts`，9 用例，全过）：
- 页面渲染 + 标题
- 9 个工具卡片可见
- 写工具带 `write` 标签
- 工具卡片展开显示 inputSchema
- API Key summary 计数 + 跳设置链接
- 无 key 时 summary 计数为 0
- 安全约束 section
- 4 个客户端配置示例
- 未登录重定向

### 6. AI 对话页（Phase 2 规划）
- 底部输入框
- AI 消息显示工具调用状态

**布局示意：**
```
┌────────┬──────────────────────────────────────────────┐
│        │  AI 对话                              [新对话] │
│        │                                              │
│        │  ┌──────────┐  ┌─────────────────────────┐  │
│        │  │ 对话历史  │  │                         │  │
│        │  │          │  │  🤖 AI                  │  │
│        │  │ 今日对话  │  │  你好！我是你的社媒发布  │  │
│        │  │ 昨日对话  │  │  助手...                │  │
│        │  │ 上周对话  │  │                         │  │
│        │  │          │  │  ──────────────────────  │  │
│        │  │          │  │                         │  │
│        │  │          │  │  👤 用户                │  │
│        │  │          │  │  帮我查看这周的计划      │  │
│        │  │          │  │                         │  │
│        │  │          │  │  🤖 AI (工具调用 ✓)      │  │
│        │  │          │  │  找到了 3 个计划...      │  │
│        │  │          │  │                         │  │
│        │  └──────────┘  ├─────────────────────────┤  │
│        │                │ 输入消息...        [发送]│  │
│        │                └─────────────────────────┘  │
└────────┴──────────────────────────────────────────────┘
```

### 7. 设置页

**设计要点：**
- 分区设置
- 用户信息（用户名、邮箱）
- AI 配置（提供商、API Key、模型）
- 主题设置（未来）

**布局示意：**
```
┌────────┬──────────────────────────────────────────────┐
│        │  设置                                          │
│        │                                              │
│        │  用户信息                                      │
│        │  ┌────────────────────────────────────────┐  │
│        │  │ 用户名: myuser                          │  │
│        │  │ 邮箱: my@email.com                      │  │
│        │  └────────────────────────────────────────┘  │
│        │                                              │
│        │  AI 配置                                      │
│        │  ┌────────────────────────────────────────┐  │
│        │  │ 提供商: OpenAI ▼                       │  │
│        │  │ API Key: ***************************** │  │
│        │  │ 模型: gpt-4 ▼                         │  │
│        │  │ [保存配置]                             │  │
│        │  └────────────────────────────────────────┘  │
│        │                                              │
│        │  账号管理                                      │
│        │  ┌────────────────────────────────────────┐  │
│        │  │ [修改密码]  [退出登录]                  │  │
│        │  └────────────────────────────────────────┘  │
└────────┴──────────────────────────────────────────────┘
```

---

## 组件清单

### 通用组件

| 组件名称 | 描述 | 状态 |
|---------|------|------|
| Button | 按钮（primary/secondary/danger） | 待实现 |
| Input | 输入框 | 待实现 |
| Select | 下拉选择框 | 待实现 |
| Modal | 弹窗 | 待实现 |
| Toast | 提示消息 | 待实现 |
| Avatar | 用户头像 | 待实现 |
| Badge | 状态标签 | 待实现 |
| Card | 卡片容器 | 待实现 |
| Table | 表格组件 | 待实现 |
| Pagination | 分页器（含每页条数选择器 + 智能省略号） | 已实现（`src/components/ui/Pagination.tsx`） |

### 业务组件

| 组件名称 | 描述 | 状态 |
|---------|------|------|
| Sidebar | 侧边导航栏 | 待实现 |
| PostEditor | 帖子编辑器 | 待实现 |
| MediaUploader | 媒体上传器 | 待实现 |
| CalendarGrid | 日历网格 | 待实现 |
| ChatWindow | AI 对话窗口 | 待实现 |
| MessageBubble | 消息气泡 | 待实现 |
| ToolCallBadge | 工具调用标识 | 待实现 |
| AccountCard | 账号卡片 | 待实现 |
| PostCard | 帖子卡片 | 待实现 |
| StatusBadge | 状态徽章 | 待实现 |

---

## 交互规范

### 状态反馈

| 状态 | 视觉反馈 |
|------|---------|
| 加载中 | 旋转图标 + 半透明遮罩 |
| 成功 | 绿色 Toast + 成功图标 |
| 错误 | 红色 Toast + 错误信息 |
| 空状态 | 空状态插图 + 引导文案 |

### 操作反馈

| 操作 | 反馈 |
|------|------|
| 按钮点击 | 缩放 + 颜色变化 |
| 表单提交 | 按钮变为 loading 状态 |
| 删除确认 | 二次确认弹窗 |
| 保存成功 | 绿色 Toast + 自动关闭 |

---

## 响应式设计

### 断点

| 断点 | 屏幕宽度 | 布局变化 |
|------|---------|----------|
| Mobile | < 640px | 单列，侧边栏收起 |
| Tablet | 640px - 1024px | 双列，侧边栏可折叠 |
| Desktop | > 1024px | 三列，完整侧边栏 |

### 移动端适配

- 侧边栏改为底部导航或汉堡菜单
- 日历视图改为垂直列表
- 表格视图改为卡片列表
- AI 对话保持全屏体验

---

## 技术实现

### 路由配置（Next.js）

```typescript
// app/(auth)/login/page.tsx
// app/(auth)/register/page.tsx
// app/(main)/page.tsx          # 仪表盘
// app/(main)/calendar/page.tsx
// app/(main)/posts/page.tsx
// app/(main)/posts/new/page.tsx
// app/(main)/posts/[id]/edit/page.tsx
// app/(main)/accounts/page.tsx
// app/(main)/ai-tools/page.tsx      # MCP 配置 + 工具列表（v0.4）
// app/(main)/trash/page.tsx         # 回收站（v0.3 起）
// app/(main)/settings/page.tsx      # API Key 管理（含 scope 选择）
```

### 状态管理（Zustand）

```typescript
// stores/authStore.ts      // 认证状态（用户信息、登录状态）
// stores/uiStore.ts        // UI 状态（侧边栏、弹窗、Toast）
// stores/chatStore.ts      // AI 对话状态（消息、对话列表）
// stores/postStore.ts      // 帖子状态（列表、当前编辑）
```

---

*文档生成时间：2026-05-31*